apply plugin: 'java'
apply plugin: 'eclipse'

project.ext.workingDir='jenkins_files'
apply from: 'jenkins_files/build.gradle'

defaultTasks 'clean', 'build'

repositories{
    mavenCentral()
}

dependencies {
        testCompile 'junit:junit:4.11'
}
